#include <iostream>
#include <bits/stdc++.h>
#include<string>
#include <fstream>

long mpow(long x, int p, int z);

long StringToIntFormat(const std::string& s);

std::string IntToStrFormat(const std::string& s);

int main();
